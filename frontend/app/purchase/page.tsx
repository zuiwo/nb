'use client';
import { Typography } from 'antd';

const { Title } = Typography;

const PurchasePage = () => {
  return (
    <div>
      <Title level={2}>进货页面</Title>
      <div style={{ marginTop: 24 }}>
        {/* 进货页面内容，后续添加 */}
      </div>
    </div>
  );
};

export default PurchasePage;
