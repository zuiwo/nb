'use client';
import { Typography } from 'antd';

const { Title } = Typography;

const CustomersPage = () => {
  return (
    <div>
      <Title level={2}>客户页面</Title>
      <div style={{ marginTop: 24 }}>
        {/* 客户页面内容，后续添加 */}
      </div>
    </div>
  );
};

export default CustomersPage;
