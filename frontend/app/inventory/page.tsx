'use client';
import { Typography } from 'antd';

const { Title } = Typography;

const InventoryPage = () => {
  return (
    <div>
      <Title level={2}>库存页面</Title>
      <div style={{ marginTop: 24 }}>
        {/* 库存页面内容，后续添加 */}
      </div>
    </div>
  );
};

export default InventoryPage;
