'use client';
import { Typography } from 'antd';

const { Title } = Typography;

const BillsPage = () => {
  return (
    <div>
      <Title level={2}>账单页面</Title>
      <div style={{ marginTop: 24 }}>
        {/* 账单页面内容，后续添加 */}
      </div>
    </div>
  );
};

export default BillsPage;
